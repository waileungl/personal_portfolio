{
    "builds": [
        {
            "src": "./server/(.*)",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "./server/(.*)",
            "dest": "/server.js"
        }
    ]
}